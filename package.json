{
    "name": "hunche",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "concurrently 'node-sass  -w -r --output-style compact ./src/scss -o ./src/css' 'browser-sync start --server ./src --files '**/*.html,**/*.css'' ",
        "test": "echo \"Error: no test specified\" && exit 1",
        "http-serve": "http-server ./src -o -c-1",
        "serve": "concurrently 'node-sass  -w -r --output-style compact ./src/scss -o ./src/css' 'browser-sync start --server ./src --files '**/*.html,**/*.css'' "
    },
    "author": {
        "name": "zane",
        "email": "zzg9583@gmail.com"
    },
    "license": "ISC",
    "devDependencies": {
        "browser-sync": "^2.26.5",
        "http-server": "^0.11.1",
        "node-sass": "^4.12.0"
    },
    "dependencies": {
        "concurrently": "^4.1.0"
    }
}